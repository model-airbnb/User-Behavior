// DataSimulation.js

getRandomUserId should return a random integer between 1 and 100000

getRandomNumOfVisits should return a random integer between 1 and 30

getRandomNumOfHits should return a random integer between 1 and 25

countUsersByVisits should increment by one at the respective key when a new user is added

getChance should return a random float between 0 and 100

findNumBookingsForMaxTwo should return a number of bookings between 0 and 2

getBookingsForOne should return 1 if chance < 3 and 0 if chance > 3

getBookingsForTwoToSeven should return [appropriate num of bookings];


